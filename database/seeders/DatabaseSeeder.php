<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seeder calls are appended here as each module is completed.
     * Module 2 (Roles & Permissions) will add:
     *   - RolePermissionSeeder (Super Admin, Admin, Manager, Cashier,
     *     Kitchen, Delivery roles + full permission matrix)
     *   - AdminUserSeeder (default super-admin login)
     */
    public function run(): void
    {
        //
    }
}